# 依赖与构建产物
node_modules/
package-lock.json

# 运行时数据（含用户埋点，不要提交）；但白名单是配置，要跟着仓库走
server/data/*
!server/data/direct-hosts.txt

# 本地打包产物（云托管部署包里有密钥，绝对不能提交）
dist/

# 环境变量（含密钥，绝对不能提交）
server/.env
.env

# 微信开发者工具的本地设置
miniprogram/project.private.config.json
miniprogram/node_modules/
/project.private.config.json
project.private.config.json

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
